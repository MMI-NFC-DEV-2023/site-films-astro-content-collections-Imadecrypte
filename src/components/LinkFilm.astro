---
import type { CollectionEntry } from "astro:content";

type Props = CollectionEntry<"films">;

const file = Astro.props;
---

<a href={'/films/${film.id}'} class="text-blue-500 hover:underline">
    {file.data.titre}
</a>